rules_version = '2';

// Chat data is only ever read/written by the Cloud Functions (Admin SDK,
// which bypasses these rules entirely). No client should ever touch
// Firestore directly, so deny everything.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
